# Variables d'environnement pour l'application Harry Potter Spell Recognition
# Copiez ce fichier en .env et personnalisez les valeurs

# Clé secrète Flask (obligatoire en production)
# Générez une clé forte avec : python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=votre_cle_secrete_super_forte_ici

# Configuration Flask
FLASK_ENV=development
FLASK_DEBUG=false

# Port d'écoute (optionnel, défaut: 5000)
PORT=5000